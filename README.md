# NOIVaulter
AI oracle vaulting NOI from energy data. Predicts $/unit savings via Random Forest on MF features.
## Setup
```bash
pip install pandas scikit-learn
```
## Usage
Run `vault.py` with CSV inputs.
MIT. Contribute: Weather API hooks!